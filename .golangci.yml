run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - unconvert
    - goconst
    - bodyclose

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
        - goconst
